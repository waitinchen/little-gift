# 生产环境配置模板
# 复制此文件为 .env 并填写实际值

NODE_ENV=production
PORT=9000

# 数据库连接（Railway PostgreSQL）
DATABASE_URL=postgres://<user>:<pass>@<host>:<port>/<db>

# 安全密钥（使用 openssl rand -base64 32 生成）
JWT_SECRET=<your-jwt-secret>
COOKIE_SECRET=<your-cookie-secret>

# CORS 配置（替换为您的实际域名）
MEDUSA_ADMIN_CORS=https://admin.little-gift.tw,http://localhost:7001
MEDUSA_STORE_CORS=https://little-gift.tw,https://your-vercel-domain.vercel.app,http://localhost:3000

# 后端服务地址
MEDUSA_BACKEND_URL=https://api.little-gift.tw

# Redis（可选）
# REDIS_URL=redis://default:<pass>@<host>:6379

# AI API（用于礼物推荐和卡片生成）
# 使用您选择的 LLM 服务
OPENAI_API_KEY=<your-openai-key>
# 或者使用其他 LLM 服务
